package com.sabbir.tagyourplace;

import android.content.UriMatcher;
import android.net.Uri;

public class ContentDescriptor {
	//AUTHORITY  & URI both identifies the ContentProvider
/*	public static final String AUTHORITY="com.sabbir.tagyourplace";
	private static final Uri CONTENT_URI = Uri.parse("content://"+AUTHORITY);
	
	public static final UriMatcher URI_MATCHER = buildUriMatcher();
	
	//creates an instance of URIMatcher for the ContentProvider
	private static UriMatcher buildUriMatcher() 
	{
		final UriMatcher matcher = new UriMatcher(UriMatcher.NO_MATCH);
		final String authority = AUTHORITY;
		
		matcher.addURI(authority, path, code)
		return null;
	}*/
}
